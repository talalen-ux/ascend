# Address of the deployed AscendEngine. Until this is set to a real
# address, the dapp runs in read-only demo mode (bootstrap state).
NEXT_PUBLIC_ASCEND_ENGINE=

# 1 = Ethereum mainnet, 11155111 = Sepolia.
NEXT_PUBLIC_CHAIN_ID=1
